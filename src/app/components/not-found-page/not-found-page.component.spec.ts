import { NotFoundPageComponent } from './not-found-page.component';

describe('NotFoundPageComponent', () => {
  let component: NotFoundPageComponent;

  beforeEach(() => {
    component = new NotFoundPageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
